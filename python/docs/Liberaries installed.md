# Liberaries to install if you want to use the same setup as me
- CustomTkinter:
```sh { closeTerminalOnSuccess=false }
pip install CustomTkinter
```
```sh { closeTerminalOnSuccess=false }
pip3 install CustomTkinter
```

